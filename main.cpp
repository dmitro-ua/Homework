#include "Header.h"

void main()
{
	CCar pajero(jip);
	pajero.SetFare(30);
	pajero.Info();
	
	CCar gazel(gazel);
	gazel.SetFare(3);
	gazel.Info();

	CTrain UKRZ(plackart);
	UKRZ.SetFare(170);
	UKRZ.Info();
	_getch();
}
	
